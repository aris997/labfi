#include <stdlib.h>
#include <stdio.h>



int main () {
	int i, j;
	FILE *output;
	char nome[12];
	for (i=0; i<5; i++) {
		sprintf(name, "output%d2.")
		output = fopen(nome, "w");
		for (j=0; j<5; j++) {	fprintf(output, "Hello World!\n");	}
		fclose(output);
	}

	exit(0);
}